<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="system.mapper.UserMapper">
    <select id="selectByup" resultType="system.entity.User1">
        SELECT * FROM user1 WHERE username = #{username} AND password = #{password}
    </select>
    <select id="selectByname" resultType="system.entity.User1">
        SELECT * FROM user1 WHERE username = #{username}
    </select>
        </mapper>